apply plugin: 'groovy'

repositories {
    mavenCentral()
    maven {
        url "https://plugins.gradle.org/m2/"
    }
}
dependencies {
    compile "org.tmatesoft.svnkit:svnkit:1.8.15"
    compile gradleApi()
    compile localGroovy()
}

